# @os-theme/darwin-arm64

Prebuilt macOS ARM64 binary for [os-theme](https://github.com/basiclines/os-theme).

This package is automatically installed as an optional dependency of `os-theme`. You should not need to install it directly.
